<%- include('../partials/head',{title:"Login"}) %>

    <body>
        <header class="header">
             <h1>Aire Libre</h1>
            <h2>Login</h2>
            <p>Vive tu propia Aventura!!!</p>
                
                    <a href="/users/login"><i class="fa-solid fa-gear"></a></i>
        </header>

        <body>
            <main>

                <a href="/">
                    <button class="volver">Volver</button>
                    </a>
                <form class="form" action="../users/identity" method="post">
                   
                    <div class="form-login">
                        <label for="correo">Correo electronico</label>
                        <input type="text" name="correo">
                        <p>
                            <%= locals.errors && errors.correo ? errors.correo.msg : null %>
                        </p>
                    </div> 
                     <div class="form-login">
                        <label for="contrasena">Contraseña</label>
                        <input type="password" name="contrasena">
                        <p>
                            <%= locals.errors && errors.contrasena ? errors.contrasena.msg : null %> 
                        </p>
                    </div>
                         <div>
                        <button  class="ingresar" type="submit">Ingresar</button>
                        </div>

                         <div class="session">
                            <input type="checkbox" name="recordar">
                            <label for="recordar" name="recordar" >Desea recordar su session?</label>
                         </div>
                         <% if (locals.error) { %>
                            
                                <%= error %>
                                <% } %>    
                         <div>
                        
                            
                        
                        </div>


                        <div class="olvidaste">
                            <p>¿Olvidaste tu contraseña?</p>
                            <button class="ingresar" type="submit">Recuperar contraseña</button>
                        </div>
              </form>
            </main>
            <%- include('../partials/footer') %>
        </body>

        </html>